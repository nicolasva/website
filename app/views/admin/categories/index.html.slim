- model_class = Category
.page-header
  h1
    = t(".title")

= form_tag :action => 'homepage', :controller => 'categories' do
  table.table.table-striped
    thead
      tr
        th.col-md-2= model_class.human_attribute_name(:title)
        th.col-md-2= model_class.human_attribute_name(:english_title)
        th.col-md-2= model_class.human_attribute_name(:sub_menu)
        th.col-md-2= ContentsOrAnnoncment.human_attribute_name(:content_or_annoncment)
        th.col-md-2= HomepageSubmenu.human_attribute_name(:homepage)
        th.col-md-2= t '.actions', :default => t("helpers.actions")
  ul#sortable.list-unstyled.common_table.category_list
    - @categories.each do |category|
      = render(:partial => "/admin/categories/list_category", :locals => { :category => category })
  .row
    .col-md-10
      = link_to t(".new", :default => t("helpers.links.new")), new_admin_title_path, :class => 'btn btn-primary', remote: true
    .col-md-2
      = submit_tag t("admin.common.valid"), name: "allcategory_valid_general", :class => "btn btn-success"
